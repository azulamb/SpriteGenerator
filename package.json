{
  "name": "spritegenerator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run ins & npm run rbuild & npm run mbuild",
    "rbuild": "tsc --project src/render",
    "mbuild": "tsc --project src/main",
    "ins": "cd App & npm i",
    "start": "electron App",
    "release": "electron-packager App spritegenerator --overwrite --platform=win32 --arch=x64 --icon=icon.ico --electron-version=1.6.2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hiroki",
  "license": "MIT",
  "devDependencies": {
    "@types/electron": "1.4.33"
  }
}
